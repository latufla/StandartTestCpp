#pragma once

namespace sg {
	// it should be just IInfo if quests etc.
	// this is the class for object desc. with all public properties
	class IGameObjectInfo {
	public:
		// if we load external config, there should be overloaded update for 
		// all properties
	};
}
